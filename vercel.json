{
  "functions": {
    "api/predict.py": {
      "memory": 1024,
      "maxDuration": 10
    }
  },
  "builds": [
    { "src": "api/predict.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/predict.py" }
  ]
}

# 基础依赖 - Python 3.12兼容版本
setuptools>=68.0.0
wheel>=0.41.0
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
joblib>=1.3.0
flask>=2.3.2
gunicorn>=21.2.0

# 精简的文本处理依赖
# 注意：完整的sentence-transformers和torch会显著增加部署包大小
# 如果您只需要基本预测功能，可以考虑移除这些
sentence-transformers>=2.2.2

# 核心工具
tqdm>=4.65.0
